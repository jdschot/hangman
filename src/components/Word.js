import React, { Component } from 'react';
import { connect } from 'react-redux'

class Word extends Component {

  // render
}

const mapStateToProps = ({  }) => ({ correctGuesses, newGame })
export default connect(mapStateToProps, {})(Word)
